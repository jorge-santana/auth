services:
  auth:
    container_name: auth
    image: auth
    build:
      context: .
    env_file:
      - .env
    ports:
      - 3000:3000
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev"
